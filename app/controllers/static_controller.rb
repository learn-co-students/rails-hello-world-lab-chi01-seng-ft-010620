class StaticController < ApplicationController

    def hello_world
        # if the following code (render "hello_world") is put here, that would be EXPLICIT rendering. Since there ins't any code, it is Implicit Rendering
    end
end